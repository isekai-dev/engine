export default ({
    command: `shop [add|remove] [shop]`,
    help: `Add a shop to pull skills from`,

    handler: () => {
        /*
         * npm install --save [shop]
         * npm uninstall --save [shop]
         */
    }
});